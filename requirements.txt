# Web Framework (Essential)
Flask==3.0.0
gunicorn==21.2.0

# Database ONLY (Remove pandas to save 50-80MB)
asyncpg>=0.30.0,<1.0.0
psycopg2-binary>=2.9.7,<3.0.0

# Minimal Data Processing (numpy only, NO pandas)
numpy>=1.26.0,<2.0.0

# Machine Learning (CPU-only, minimal)
torch>=2.1.2,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.3.0,<3.0.0

# Utilities ONLY
python-dotenv>=1.0.0,<2.0.0